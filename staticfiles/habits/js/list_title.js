const projectitle = document.getElementById('project-title');
const $project_link = Array.prototype.slice.call(document.querySelectorAll('.link'), 0);
let current_title = document.addEventListener.innerHTML


function changeTitle(args) {
    projectitle.innerHTML = args;
};



